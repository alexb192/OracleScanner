@AGENTS.md

# Package manager

**pnpm** is the primary package manager for this project. Use `pnpm add <pkg>` to install dependencies — `npm install` will fail with an arborist error due to the pnpm lockfile layout.
